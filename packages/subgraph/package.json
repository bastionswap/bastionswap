{
  "name": "@bastionswap/subgraph",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph codegen && graph build",
    "deploy:testnet": "graph deploy --studio bastionswap-base-sepolia",
    "deploy:mainnet": "graph deploy --studio bastionswap-base",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.80.0",
    "@graphprotocol/graph-ts": "^0.35.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.6.0"
  }
}
